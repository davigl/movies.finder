import styled from 'styled-components';

import { colors } from '~/assets/styles/partials/_variables';

const TitleFilmDetails = styled.h3`
  margin-bottom: 15px;

  color: ${colors.white};
  font-weight: 600;
  font-size: 1.3em;
`;

export default TitleFilmDetails;
